import { IdToAvatarPipe } from './id-to-avatar.pipe';

describe('IdToAvatarPipe', () => {
  it('create an instance', () => {
    // @ts-ignore
    const pipe = new IdToAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
