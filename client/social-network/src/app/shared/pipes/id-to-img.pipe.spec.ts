import { IdToImgPipe } from './id-to-img.pipe';

describe('IdToImgPipe', () => {
  it('create an instance', () => {
    // @ts-ignore
    const pipe = new IdToImgPipe();
    expect(pipe).toBeTruthy();
  });
});
